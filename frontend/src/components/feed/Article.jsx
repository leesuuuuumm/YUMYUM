import React from 'react';
import PropTypes from 'prop-types';

function Article(props) {
  return (
    <section className="login">
      <div className="userContainer">
        <h2 className="userAppTitle">게시글!</h2>
      </div>
    </section>
  );
};

Article.propTypes = {
  
};

export default Article;